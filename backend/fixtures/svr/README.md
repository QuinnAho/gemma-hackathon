SVR backend regression fixtures.

Each fixture directory contains:
- `manifest.json`
- `events.jsonl`
- `expected.json`

These are curated deterministic replay fixtures for the backend assessment/export harness.
They are committed test data, not machine-local runtime logs.
